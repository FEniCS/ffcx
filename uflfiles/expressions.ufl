"""
An example .ufl file exporting multiple expressions.
"""

cell = cell2D

U = FiniteElement("CG", cell, 1)
V = VectorElement("CG", cell, 1)
W = TensorElement("CG", cell, 1)
S = TensorElement("CG", cell, 1, symmetry=True)

u = Coefficient(U)
v = Coefficient(V)
w = Coefficient(W)
s = Coefficient(S)

f = 2*u
g = dot(v,v)
h = dot(w, div(s))

expressions = [f, g, h]
