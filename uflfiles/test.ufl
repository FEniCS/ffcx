"""
An example .ufl file exporting multiple elements,
coefficients, expressions, and functionals.
"""

cell = cell2D

U = FiniteElement("CG", cell, 1)
V = VectorElement("CG", cell, 1)
W = TensorElement("CG", cell, 1)
S = TensorElement("CG", cell, 1, symmetry=True)

x, y = SpatialCoordinate(cell)

u = Coefficient(U)
v = Coefficient(V)
w = Coefficient(W)
s = Coefficient(S)

f = 2*u
g = dot(v,v)
h = u*dot(v, dot(w, div(s)))

a = f*dx
b = g*dx
c = h*dx

elements = [U, V, W, S]
coefficients = [u, v, w, s]
forms = [a, b, c]
expressions = [f, g, h]
