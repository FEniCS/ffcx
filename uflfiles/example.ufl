domain = Domain(triangle)

V = FiniteElement("CG", domain, 1)

x, y = SpatialCoordinate(domain)

u = TrialFunction(V)
v = TestFunction(V)
f = Coefficient(V)
c = Constant(domain)

x = SpatialCoordinate(domain)

a = (x[0]*f*u)*(c*v*x[1])*dx
#a = (f*u)*(c*v)*dx

