# Copyright (c) 2005-2006 Anders Logg (logg@tti-c.org)
# Licensed under the GNU GPL Version 2
#
# This example demonstrates the use of projections.
#
# Compile this form with FFC: ffc Projection.form

P0 = FiniteElement("Discontinuous Lagrange", "triangle", 0)
P1 = FiniteElement("Lagrange", "triangle", 1)
P2 = FiniteElement("Lagrange", "triangle", 2)

v = BasisFunction(P1)
f = Function(P1)

pi0 = Projection(P0)
pi1 = Projection(P1)
pi2 = Projection(P2)

L = v*(pi0(f) + pi1(f) + pi2(f))*dx
