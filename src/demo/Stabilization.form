# Copyright (c) 2005 Anders Logg (logg@tti-c.org)
# Licensed under the GNU GPL Version 2
#
# The bilinear form for a part of the stabilization term in
# a least-squares stabilized cG(1)cG(1) formulation of the
# incompressible Navier-Stokes equations.
#
# Compile this form with FFC: ffc Stabilization.form

element = FiniteElement("Vector Lagrange", "tetrahedron", 1)

v = BasisFunction(element)
u = BasisFunction(element)
w = Function(element)

a = w[j]*v[i].dx(j) * w[k]*u[i].dx(k) * dx
