# Copyright (c) 2005 Anders Logg (logg@tti-c.org)
# Licensed under the GNU GPL Version 2
#
# The bilinear form a(u,v) and linear form L(v) for
# Poisson's equation in system form (vector-valued).
#
# Compile this form with FFC: ffc poissonsystem.form.

name = "PoissonSystem"
element = FiniteElement("Vector Lagrange", "triangle", 1)

v = BasisFunction(element)
u = BasisFunction(element)
f = Function(element)

a = v[i].dx(j)*u[i].dx(j)*dx
L = v[i]*f[i]*dx
