element = FiniteElement("Lagrange", "triangle", 1)

v = TestFunction(element)
u = TrialFunction(element)
f = Function(element)
g = Function(element)

L = sqrt(f)*f*v*dx + f*g*v*dx
#+ f*v*dx
#+ (1.0/f)*sqrt(g)*2.0*v*dx
