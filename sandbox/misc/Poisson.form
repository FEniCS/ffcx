#element = FiniteElement("Discontinuous Lagrange", "triangle", 1)
element = FiniteElement("Lagrange", "triangle", 1)

v = TestFunction(element)
u = TrialFunction(element)

a = dot(grad(v), grad(u))*dx + 0.0*v*u*dx
